# Security Model

Risky actions require human approval: shell commands, file deletion, network access, GitHub push, package installation, API-key usage, code execution, scheduled autonomy, external communication, voice model export, memory deletion, and workspace deletion. GAIA uses a permission manager, policy engine, guards, audit log, secrets manager, rollback manager, self-modification guard, and kill switch.
