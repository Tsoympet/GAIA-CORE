# Audio Pipeline

Audio flows from microphone input to wake-word detection, STT, voice router, agent execution, safety filtering, TTS, speaker output, transcript storage, and audit logs. The bootstrap system keeps microphone and speaker disabled by default.
