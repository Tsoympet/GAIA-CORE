# Speech Interface

Speech input uses a Whisper-compatible abstraction and text fallback. Speech output uses a local-first TTS abstraction and must pass through voice safety filters before any audio is generated.
