# Memory System

The bootstrap memory system is append-only and local-first. Future phases add scoped workspace memory, symbolic memory, vector indexes, retention policies, deletion approvals, and audit trails.
